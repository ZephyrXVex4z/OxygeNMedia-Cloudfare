(()=>{const O=window.OxygeNTools;O.register('uuid',host=>{host.innerHTML=`<section class="ot-panel"><div class="ot-fields"><div class="ot-field"><label for="uuid-count">Cantidad</label><select class="ot-select" id="uuid-count"><option value="1">1 UUID</option><option value="5">5 UUID</option><option value="10">10 UUID</option><option value="50">50 UUID</option><option value="100">100 UUID</option></select></div></div><div class="ot-actions"><button class="ot-btn primary" id="uuid-generate">Generar UUID</button><button class="ot-btn" id="uuid-copy">Copiar todos</button><button class="ot-btn ghost" id="uuid-clear">Limpiar</button></div><div class="ot-field" style="margin-top:16px"><label for="uuid-output">UUID generados</label><textarea class="ot-textarea mono" id="uuid-output" readonly aria-live="polite" placeholder="Tus UUID aparecerán aquí…"></textarea></div><div class="ot-message" aria-live="polite"></div></section>`;const $=s=>O.$(s,host);function generate(){if(!crypto?.getRandomValues)return O.msg(host,'Este navegador no ofrece aleatoriedad segura. Abre la página en una conexión HTTPS actualizada.');const count=Number($('#uuid-count').value);const ids=Array.from({length:count},()=>{if(crypto.randomUUID)return crypto.randomUUID();const b=crypto.getRandomValues(new Uint8Array(16));b[6]=(b[6]&15)|64;b[8]=(b[8]&63)|128;const h=[...b].map(x=>x.toString(16).padStart(2,'0')).join('');return `${h.slice(0,8)}-${h.slice(8,12)}-${h.slice(12,16)}-${h.slice(16,20)}-${h.slice(20)}`;});$('#uuid-output').value=ids.join('\n');O.msg(host,`${count} UUID generado${count>1?'s':''}.`,'success');}$('#uuid-generate').addEventListener('click',generate);$('#uuid-copy').addEventListener('click',async()=>{if(!$('#uuid-output').value)return O.msg(host,'Primero genera uno o varios UUID.');try{await navigator.clipboard.writeText($('#uuid-output').value);O.msg(host,'UUID copiados.','success')}catch{O.msg(host,'No se pudo copiar automáticamente.')}});$('#uuid-clear').addEventListener('click',()=>{$('#uuid-output').value='';O.clearMsg(host);});generate();});})();
